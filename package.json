{
  "name": "chess-with-friends",
  "private": true,
  "type": "module",
  "scripts": {
    "test:frontend": "npm --prefix frontend test",
    "lint": "npm --prefix frontend exec -- eslint frontend tests/frontend",
    "build:frontend": "npm --prefix frontend run build",
    "test:backend": "uv run --directory backend pytest ../tests/backend"
  }
}
